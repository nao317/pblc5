#ifndef GAIT_PARAMETERS_H
#define GAIT_PARAMETERS_H

struct GaitParameters {
    float stepLength;
    float stepHeight;
    float cycleTime;
    float bodyHeight;
    float turnRate;
    bool climbingMode;
};

#endif
